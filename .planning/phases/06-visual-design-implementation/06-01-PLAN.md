---
phase: 06-visual-design-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/components/ui/nav-bar.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/kpi-card.tsx
  - src/components/ui/health-indicator.tsx
  - src/components/ui/refresh-button.tsx
  - src/components/ui/empty-state.tsx
autonomous: true

must_haves:
  truths:
    - "All pages render with Cormorant Garamond for headings and DM Sans for body text"
    - "Color palette uses Paper/Ink editorial theme variables throughout"
    - "NavBar has dark ink background with accent-colored Skyvera logo"
    - "Shared UI components (Card, Badge, KPICard, HealthIndicator) use editorial theme colors"
  artifacts:
    - path: "src/app/globals.css"
      provides: "CSS custom properties for editorial theme, Google Fonts import"
      contains: "--paper.*#fafaf8"
    - path: "tailwind.config.ts"
      provides: "Extended theme with editorial color palette and font families"
      contains: "cormorant"
    - path: "src/app/layout.tsx"
      provides: "Google Fonts loaded via next/font or link tag, applied to body"
      contains: "Cormorant"
    - path: "src/components/ui/nav-bar.tsx"
      provides: "Dark ink navbar with accent logo matching reference global-nav"
      min_lines: 30
  key_links:
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variables consumed by Tailwind theme extension"
      pattern: "--ink|--paper|--accent|--secondary"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "Font classes applied to html/body"
      pattern: "font-sans|DM.Sans|Cormorant"
---

<objective>
Establish the editorial design system foundation: CSS custom properties, Google Fonts (Cormorant Garamond + DM Sans), Tailwind theme extension, and restyle all shared UI components to use the Paper/Ink editorial theme.

Purpose: Every page inherits the editorial theme from this foundation. All subsequent plans depend on these tokens being available.
Output: Updated globals.css, tailwind.config.ts, layout.tsx, and all 7 shared UI components restyled.
</objective>

<execution_context>
@/Users/RAZER/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RAZER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-visual-design-implementation/06-CONTEXT.md

Reference design CSS variables (from output/index.html and output/Liquid_Telecom.html):
```css
:root {
    --ink: #1a1a1a;
    --paper: #fafaf8;
    --accent: #c84b31;
    --secondary: #2d4263;
    --muted: #8b8b8b;
    --border: #e8e6e1;
    --highlight: #ecdbba;
    --success: #4caf50;
    --warning: #ff9800;
    --critical: #e53935;
}
```

Reference typography:
- `h1, h2, h3`: font-family: 'Cormorant Garamond', serif; font-weight: 600; line-height: 1.2
- `h1`: font-size: 3.5rem; font-weight: 300
- `h2`: font-size: 1.75rem; margin-bottom: 1.5rem; color: var(--secondary)
- `h3`: font-size: 1.25rem; margin-bottom: 1rem
- `body`: font-family: 'DM Sans', sans-serif; line-height: 1.6

Reference navbar:
- `.global-nav`: background: var(--ink); color: var(--paper); padding: 1rem 2rem
- `.global-nav-logo`: font-size: 1.5rem; font-weight: 700; color: var(--accent)

Current files to modify:
@src/app/globals.css
@tailwind.config.ts
@src/app/layout.tsx
@src/components/ui/nav-bar.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/kpi-card.tsx
@src/components/ui/health-indicator.tsx
@src/components/ui/refresh-button.tsx
@src/components/ui/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design system foundation - CSS variables, fonts, Tailwind config</name>
  <files>
    src/app/globals.css
    tailwind.config.ts
    src/app/layout.tsx
  </files>
  <action>
  1. **globals.css** - Replace existing CSS custom properties with editorial theme:
     - Keep `@tailwind base/components/utilities` directives
     - Replace existing `:root` variables with editorial palette:
       ```
       --ink: #1a1a1a; --paper: #fafaf8; --accent: #c84b31; --secondary: #2d4263;
       --muted: #8b8b8b; --border: #e8e6e1; --highlight: #ecdbba;
       --success: #4caf50; --warning: #ff9800; --critical: #e53935;
       ```
     - Add Google Fonts import at top of file: `@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600;700&family=DM+Sans:wght@400;500;700&display=swap');`
     - Set body: `font-family: 'DM Sans', sans-serif; background: var(--paper); color: var(--ink); line-height: 1.6`
     - Add base heading styles: `h1, h2, h3 { font-family: 'Cormorant Garamond', serif; font-weight: 600; line-height: 1.2; }`
     - Add utility classes for common patterns: `.text-accent { color: var(--accent); }` `.bg-highlight { background: var(--highlight); }` `.border-editorial { border-color: var(--border); }`

  2. **tailwind.config.ts** - Extend theme with editorial tokens:
     - Add colors: `ink`, `paper`, `accent`, `secondary`, `muted`, `border-editorial` (avoid name collision with Tailwind's `border`), `highlight`
     - Add fontFamily: `display: ['Cormorant Garamond', 'serif']`, `sans: ['DM Sans', 'sans-serif']`
     - These map to `text-ink`, `bg-paper`, `text-accent`, `font-display`, `font-sans` etc.

  3. **layout.tsx** - Apply editorial fonts to document:
     - Add `className="font-sans"` to the `<body>` tag (Tailwind v4 will use the DM Sans from config)
     - Optionally import Cormorant Garamond + DM Sans via next/font/google for optimal loading. If using next/font, create font variables and apply them. If @import in CSS is simpler and sufficient for demo, that's acceptable.
     - The body should NOT have a hardcoded background - let globals.css handle it
  </action>
  <verify>
  - Run `npm run build` -- no TypeScript or build errors
  - Open the app in browser: body text renders in DM Sans, any h1/h2/h3 renders in Cormorant Garamond
  - Inspect `:root` in DevTools and confirm all 10 CSS variables are present
  </verify>
  <done>
  - globals.css has editorial CSS custom properties and Google Fonts import
  - tailwind.config.ts extends theme with editorial colors and font families
  - layout.tsx applies font-sans class to body
  - Typography visually matches reference designs (serif headings, sans body)
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle shared UI components to editorial theme</name>
  <files>
    src/components/ui/nav-bar.tsx
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/components/ui/kpi-card.tsx
    src/components/ui/health-indicator.tsx
    src/components/ui/refresh-button.tsx
    src/components/ui/empty-state.tsx
  </files>
  <action>
  Restyle each shared component to use editorial theme. Replace inline styles and generic slate/blue Tailwind classes with editorial tokens. Keep all existing functionality, props, and component interfaces identical.

  1. **nav-bar.tsx** - Match reference `.global-nav`:
     - Background: `bg-ink` (was `bg-slate-900`)
     - Logo text: `text-accent font-bold text-2xl` with "SKYVERA" in uppercase (was title case)
     - Subtitle: `text-paper opacity-80` (was `text-slate-400`)
     - Nav links: `text-paper/70 hover:text-paper` for inactive, `bg-white/10 text-paper` for active (was `text-slate-300/bg-slate-700`)
     - Keep all navigation logic and links unchanged

  2. **card.tsx** - Match reference `.card`:
     - Background: white, border: `border border-[var(--border)]`, shadow: `shadow-sm` (subtle, 0 2px 8px rgba(0,0,0,0.05)), border-radius: `rounded` (4px)
     - Header: remove slate background, use bottom border only
     - Title: `font-display text-lg font-semibold text-secondary` (Cormorant Garamond via font-display)
     - Replace heavy box-shadow with subtle one

  3. **badge.tsx** - Match reference `.badge`:
     - More compact: `px-3 py-1 rounded text-xs font-semibold`
     - Variant colors using editorial palette:
       - default: `bg-secondary/10 text-secondary`
       - success: `bg-[var(--success)]/20 text-[#2e7d32]`
       - warning: `bg-[var(--warning)]/20 text-[#e65100]`
       - danger: `bg-[var(--critical)]/20 text-[#c62828]`

  4. **kpi-card.tsx** - Transform to editorial metric card:
     - Replace inline styles with Tailwind classes using editorial tokens
     - Background: white, border-left: `border-l-3 border-accent` (was conditional green/red - keep conditional but use accent/critical)
     - Title: `text-xs font-semibold uppercase tracking-wider text-muted` (was hardcoded #64748b)
     - Value: `text-3xl font-display font-semibold text-secondary` (Cormorant Garamond, was hardcoded #0f172a)
     - Target text: `text-sm text-muted`
     - Delta badge: keep green/red conditional but use `var(--success)`/`var(--critical)` tones
     - Add subtle shadow: `shadow-sm`
     - Apply `border border-[var(--border)]` and `rounded` (4px radius matching reference)

  5. **health-indicator.tsx** - Update colors to editorial palette:
     - Green dot: keep `bg-green-500` (close enough to --success)
     - Yellow dot: use `bg-[var(--warning)]`
     - Red dot: use `bg-[var(--critical)]`
     - Text: use `text-ink` for label text

  6. **refresh-button.tsx** - Subtle editorial styling:
     - Text: `text-muted hover:text-ink` (was `text-slate-500 hover:text-slate-700`)
     - Keep all refresh logic and SVG unchanged

  7. **empty-state.tsx** - Editorial empty state:
     - Background: `bg-highlight/30` (was `bg-gray-50`)
     - Border: `border-2 border-dashed border-[var(--border)]`
     - Title: `font-display text-lg font-semibold text-ink`
     - Description: `text-muted`
     - Action button: `bg-accent text-white hover:bg-accent/90` (was `bg-blue-600`)
  </action>
  <verify>
  - Run `npm run build` -- no errors
  - All 7 component files still export the same function names with same prop interfaces
  - NavBar renders with dark background and accent-colored logo
  - Cards show subtle shadows with editorial borders
  - KPI cards display values in Cormorant Garamond serif font
  </verify>
  <done>
  - All 7 shared UI components use editorial theme tokens instead of generic slate/blue
  - NavBar matches reference global-nav (dark ink bg, accent logo, paper text)
  - Cards have editorial styling (subtle shadow, 4px radius, --border color)
  - KPI cards show Cormorant Garamond for values, uppercase muted labels
  - Badge variants use editorial color palette
  - No functional changes -- all props, exports, behaviors preserved
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- Browser shows DM Sans body text and Cormorant Garamond headings across all pages
- DevTools shows all 10 CSS custom properties on :root
- NavBar has dark ink background with rust-accent "SKYVERA" logo
- Cards throughout the app use editorial borders and shadows
</verification>

<success_criteria>
1. Typography: Cormorant Garamond renders for all h1/h2/h3 elements, DM Sans for body
2. Color palette: CSS variables --paper, --ink, --accent, --secondary all defined and used
3. NavBar: Matches reference global-nav with dark background and accent logo
4. Shared components: All 7 components use editorial tokens, no more generic slate/blue
5. Build: `npm run build` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-design-implementation/06-01-SUMMARY.md`
</output>
