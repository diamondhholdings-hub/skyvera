---
phase: 06-visual-design-implementation
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/accounts/[name]/page.tsx
  - src/app/accounts/[name]/_components/tab-navigation.tsx
  - src/app/accounts/[name]/_components/overview-tab.tsx
  - src/app/accounts/[name]/_components/financials-tab.tsx
  - src/app/accounts/[name]/_components/organization-tab.tsx
  - src/app/accounts/[name]/_components/strategy-tab.tsx
  - src/app/accounts/[name]/_components/competitive-tab.tsx
  - src/app/accounts/[name]/_components/intelligence-tab.tsx
  - src/app/accounts/[name]/_components/action-items-tab.tsx
  - src/app/accounts/[name]/_components/stakeholder-card.tsx
  - src/app/accounts/[name]/_components/action-card.tsx
  - src/app/accounts/[name]/_components/kanban-column.tsx
  - src/app/accounts/[name]/_components/quick-add-action.tsx
autonomous: true

must_haves:
  truths:
    - "Account plan page has hero header with gradient background and stat cards"
    - "Tab navigation is sticky with editorial active state (accent underline, accent tint background)"
    - "Tab content sections use editorial typography and card styling"
    - "Stakeholder cards have accent left border matching reference exec-card"
  artifacts:
    - path: "src/app/accounts/[name]/page.tsx"
      provides: "Account plan page with hero header and stat cards"
      contains: "gradient|stat-card|font-display"
    - path: "src/app/accounts/[name]/_components/tab-navigation.tsx"
      provides: "Sticky tab bar with editorial active state"
      contains: "sticky|accent|border-bottom"
  key_links:
    - from: "src/app/accounts/[name]/page.tsx"
      to: "src/app/accounts/[name]/_components/tab-navigation.tsx"
      via: "Renders tab navigation below hero header"
      pattern: "TabNavigation"
    - from: "src/app/accounts/[name]/_components/tab-navigation.tsx"
      to: "src/app/globals.css"
      via: "Uses editorial CSS variables for tab styling"
      pattern: "accent|secondary|border"
---

<objective>
Restyle the 7-tab account plan pages to match the Telstra/Liquid Telecom reference designs with hero header, stat cards, sticky tab navigation, and editorial tab content.

Purpose: Account plan pages are the deepest content in the platform. They must match the polished reference HTML designs that already exist for these exact accounts.
Output: Account plan page shell + 12 tab-related components restyled with editorial theme.
</objective>

<execution_context>
@/Users/RAZER/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RAZER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-visual-design-implementation/06-CONTEXT.md

Reference design (from output/Liquid_Telecom.html and output/Telstra_Corporation_Limited.html):

Hero header:
- `.header { background: linear-gradient(135deg, var(--secondary) 0%, #1a2332 100%); color: var(--paper); padding: 4rem 2rem 3rem; }`
- `.header-content { max-width: 1400px; margin: 0 auto; }`
- `.subtitle { font-size: 1.1rem; opacity: 0.85; margin-bottom: 2rem; }`
- Header stats grid: stat-cards with translucent backgrounds (Total Revenue, RR, NRR, Customer Rank)
- h1: 3.5rem, font-weight 300 (light)

Sticky tabs:
- `.nav-tabs { max-width: 1400px; margin: 0 auto; padding: 0 2rem; background: white; border-bottom: 2px solid var(--border); position: sticky; top: 0; z-index: 100; box-shadow: 0 2px 8px rgba(0,0,0,0.05); }`
- `.nav-tab { padding: 1.25rem 2rem; border-bottom: 3px solid transparent; transition: all 0.3s; font-weight: 500; color: var(--muted); }`
- `.nav-tab:hover { color: var(--secondary); background: var(--highlight); }`
- `.nav-tab.active { color: var(--accent); border-bottom-color: var(--accent); background: rgba(200,75,49,0.05); }`

Tab content patterns:
- `.card { background: white; border: 1px solid var(--border); padding: 2rem; margin-bottom: 2rem; box-shadow: 0 2px 8px rgba(0,0,0,0.05); border-radius: 4px; }`
- `.metric-box { background: var(--highlight); padding: 1.5rem; border-left: 3px solid var(--accent); }`
- `.exec-card { background: var(--paper); border: 1px solid var(--border); padding: 1.5rem; border-radius: 4px; border-left: 4px solid var(--accent); }`
- `.data-table th { background: var(--secondary); color: white; padding: 1rem; }`
- `.data-table tr:hover { background: var(--highlight); }`

Current files:
@src/app/accounts/[name]/page.tsx
@src/app/accounts/[name]/_components/tab-navigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero header with stat cards and sticky tab navigation</name>
  <files>
    src/app/accounts/[name]/page.tsx
    src/app/accounts/[name]/_components/tab-navigation.tsx
  </files>
  <action>
  1. **accounts/[name]/page.tsx** - Hero header matching reference:
     - Replace flat `p-6` layout with structured hero:
       a. Back link above header: `max-w-[1400px] mx-auto px-8 pt-4` with `text-accent hover:text-accent/80` (was blue)
       b. Hero header: `bg-gradient-to-br from-secondary to-[#1a2332] text-paper px-8 pt-12 pb-10`
       c. Header content wrapper: `max-w-[1400px] mx-auto`
       d. Customer name h1: `font-display text-4xl font-light text-paper mb-2`
       e. Subtitle line: `text-paper/80 text-lg mb-6` showing "Strategic Account Plan | {BU} Business Unit | Q1 2026"
       f. Stat cards grid below name: `grid grid-cols-2 md:grid-cols-4 gap-6 mt-6`
          Each stat card: `bg-white/10 p-5 rounded border border-white/10`
          Stat label: `text-xs uppercase tracking-wider text-paper/70 mb-2`
          Stat value: `text-2xl font-display font-semibold text-paper`
          Show: Total Revenue (from customer.rr + customer.nrr), Recurring Revenue, Non-Recurring Revenue, Health Score
       g. Health indicator and BU badge in the subtitle area (next to the subtitle text)
     - Tab navigation renders below header (full-width, sticky)
     - Tab content in: `max-w-[1400px] mx-auto px-8 py-6`
     - Keep all data fetching, error handling, Suspense boundaries, and tab rendering logic unchanged

  2. **tab-navigation.tsx** - Sticky editorial tabs:
     - Desktop tab bar: Replace current styling with:
       - Wrapper: `sticky top-0 z-50 bg-white border-b-2 border-[var(--border)] shadow-sm`
       - Inner: `max-w-[1400px] mx-auto px-8 flex overflow-x-auto`
       - Each tab button: `py-5 px-6 border-b-3 border-transparent font-medium text-muted whitespace-nowrap transition-all duration-300`
       - Tab hover: `hover:text-secondary hover:bg-highlight`
       - Tab active: `text-accent border-accent bg-accent/5` (accent underline + faint accent background)
     - Mobile select: style with editorial border/focus colors
       - `border-2 border-[var(--border)] rounded-lg focus:border-accent focus:ring-accent`
     - Keep all navigation logic (useSearchParams, useRouter, tab change handler) unchanged
  </action>
  <verify>
  - Run `npm run build` -- no errors
  - Account plan page shows gradient hero header with customer name in large serif font
  - Stat cards show revenue figures in translucent-bg cards within header
  - Tab navigation sticks to top on scroll with editorial active state
  - Clicking tabs still updates URL params and swaps content
  </verify>
  <done>
  - Hero header: gradient background, serif title, stat cards matching reference
  - Tab navigation: sticky, editorial active state with accent underline and tint
  - All tab switching and URL state functionality preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle all 7 tab content components</name>
  <files>
    src/app/accounts/[name]/_components/overview-tab.tsx
    src/app/accounts/[name]/_components/financials-tab.tsx
    src/app/accounts/[name]/_components/organization-tab.tsx
    src/app/accounts/[name]/_components/strategy-tab.tsx
    src/app/accounts/[name]/_components/competitive-tab.tsx
    src/app/accounts/[name]/_components/intelligence-tab.tsx
    src/app/accounts/[name]/_components/action-items-tab.tsx
    src/app/accounts/[name]/_components/stakeholder-card.tsx
    src/app/accounts/[name]/_components/action-card.tsx
    src/app/accounts/[name]/_components/kanban-column.tsx
    src/app/accounts/[name]/_components/quick-add-action.tsx
  </files>
  <action>
  Apply editorial theme to all tab content components. For each component, replace generic slate/blue Tailwind classes with editorial tokens. Preserve ALL functionality, state management, and component interfaces.

  **Pattern replacements across all components:**
  - `bg-white rounded-lg shadow-sm border border-slate-200 p-6` -> `bg-white border border-[var(--border)] p-6 rounded shadow-sm`
  - `text-slate-900` -> `text-ink`
  - `text-slate-600`/`text-slate-500` -> `text-muted`
  - `text-blue-600 hover:text-blue-800` -> `text-accent hover:text-accent/80`
  - `bg-blue-600` -> `bg-accent`
  - `border-blue-600` -> `border-accent`
  - `bg-slate-100` -> `bg-highlight/50`
  - Section headings: add `font-display` class for Cormorant Garamond

  **Component-specific changes:**

  1. **overview-tab.tsx** - Editorial overview cards:
     - Section headings: `font-display text-xl font-semibold text-secondary mb-4`
     - Metric boxes: `bg-highlight p-5 border-l-3 border-accent` (matching reference .metric-box)
     - Intelligence report section: editorial card wrapper

  2. **financials-tab.tsx** - Editorial financial tables:
     - Table headers: `bg-secondary text-white p-4 text-left font-semibold`
     - Table rows: `hover:bg-highlight` with `border-b border-[var(--border)]`
     - Financial values in serif: `font-display font-semibold`

  3. **organization-tab.tsx** - Editorial org chart:
     - Stakeholder cards: border-left `border-l-4 border-accent` matching reference `.exec-card`
     - Role labels: `text-xs uppercase text-accent font-bold`
     - Names: `font-display text-lg font-semibold text-secondary`

  4. **stakeholder-card.tsx** - Match reference `.exec-card`:
     - Card: `bg-paper border border-[var(--border)] p-5 rounded border-l-4 border-l-accent`
     - Role: `text-xs uppercase tracking-wider text-accent font-bold mb-2`
     - Name: `font-display text-xl font-semibold text-secondary mb-3`
     - Details: `text-sm text-ink`

  5. **strategy-tab.tsx** - Editorial strategy cards:
     - Pain points/opportunities: cards with editorial borders
     - Priority labels: accent colored, uppercase
     - Status indicators using editorial success/warning/critical

  6. **competitive-tab.tsx** - Editorial competitive table:
     - Table styling same as financials (secondary header, highlight hover)
     - Competitor badges with editorial colors

  7. **intelligence-tab.tsx** - Editorial intelligence display:
     - Insight cards with highlight backgrounds and accent borders
     - News items with editorial timestamps and source labels
     - Sentiment indicators using success/warning/critical

  8. **action-items-tab.tsx** - Editorial Kanban:
     - Column headers: `font-display text-lg font-semibold text-secondary`
     - Keep @dnd-kit functionality completely untouched

  9. **action-card.tsx** - Editorial action cards:
     - Card: `bg-white border border-[var(--border)] rounded p-4 shadow-sm`
     - Priority badges: High=`bg-critical/20 text-[#c62828]`, Medium=`bg-warning/20 text-[#e65100]`, Low=`bg-success/20 text-[#2e7d32]`
     - Due dates past: `text-critical` (was `text-red-xxx`)

  10. **kanban-column.tsx** - Editorial columns:
      - Column background: `bg-paper/50` or `bg-highlight/20`
      - Column header: editorial styling
      - Keep @dnd-kit drop zone logic untouched

  11. **quick-add-action.tsx** - Editorial form:
      - Input borders: `border-[var(--border)] focus:border-accent focus:ring-accent`
      - Submit button: `bg-accent text-white hover:bg-accent/90`
  </action>
  <verify>
  - Run `npm run build` -- no errors
  - Navigate to a hero account (e.g., British Telecommunications) and check each of the 7 tabs
  - Overview tab: editorial metric boxes with accent left border
  - Organization tab: stakeholder cards have accent left border, serif names
  - Action Items tab: Kanban drag-and-drop still works, cards have editorial styling
  - All tab content uses Cormorant Garamond for headings, DM Sans for body
  </verify>
  <done>
  - All 11 tab-related components restyled with editorial theme
  - Section headings use Cormorant Garamond (font-display)
  - Tables use secondary headers, highlight hover rows
  - Stakeholder cards match reference exec-card pattern
  - Action items Kanban preserves all @dnd-kit drag-and-drop functionality
  - No functional changes to any component logic or state management
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Account plan pages load with hero header, sticky tabs, editorial content
- All 7 tabs display correctly with editorial styling
- Drag-and-drop on Action Items tab still functions
- Tab URL state (searchParams) still works
</verification>

<success_criteria>
1. Hero header: Gradient with stat cards, serif customer name, BU/health indicators
2. Sticky tabs: White background, accent active state, smooth transitions
3. All 7 tab contents: Editorial typography, cards, tables, and badges
4. Stakeholder cards: Accent left border, serif names, uppercase roles
5. Kanban: Editorial styling with @dnd-kit functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-design-implementation/06-04-SUMMARY.md`
</output>
