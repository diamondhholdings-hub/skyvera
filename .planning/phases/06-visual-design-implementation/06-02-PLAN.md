---
phase: 06-visual-design-implementation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/dashboard/components/kpi-section.tsx
  - src/app/dashboard/components/bu-breakdown.tsx
  - src/app/dashboard/components/chart-wrappers.tsx
  - src/app/dashboard/components/recent-alerts-preview.tsx
  - src/app/dashboard/components/revenue-chart.tsx
  - src/app/dashboard/components/margin-comparison.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard has gradient header section (secondary to dark) with page title in Cormorant Garamond"
    - "KPI metric cards have editorial styling with serif values and accent left border"
    - "BU breakdown uses editorial card styling with accent-colored headers"
    - "Charts section has editorial card wrappers with subtle shadows"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard with gradient header matching Financial Dashboard reference"
      contains: "gradient"
    - path: "src/app/dashboard/components/kpi-section.tsx"
      provides: "KPI grid using editorial-styled KPICard"
      min_lines: 20
    - path: "src/app/dashboard/components/bu-breakdown.tsx"
      provides: "BU cards with editorial colors and typography"
      contains: "font-display|accent|secondary"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/app/globals.css"
      via: "Uses CSS custom properties for gradient and colors"
      pattern: "--secondary|--paper|--ink"
---

<objective>
Restyle the Executive Dashboard page to match the Financial Dashboard reference design with gradient header, editorial metric cards, and proper visual hierarchy.

Purpose: The dashboard is the primary landing page for executives. It must immediately convey the editorial sophistication of the reference designs.
Output: Dashboard page and all 6 sub-components restyled with editorial theme.
</objective>

<execution_context>
@/Users/RAZER/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RAZER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-visual-design-implementation/06-CONTEXT.md

Reference design patterns (from Skyvera_Financial_Dashboard_Q126.html):
- Header: gradient background (secondary to dark), centered title, subtitle below
- Cards: rgba backgrounds with subtle borders, 15px radius in dark theme but editorial uses 4px

Reference design patterns (from output/index.html):
- Header: `background: linear-gradient(135deg, var(--secondary) 0%, #1a2332 100%); color: var(--paper); padding: 4rem 2rem 3rem`
- Header h1: `font-size: 3.5rem; font-weight: 300`
- Stat cards in header: `background: rgba(255,255,255,0.1); padding: 1.5rem; border-radius: 4px`
- Container: `max-width: 1400px; margin: 3rem auto; padding: 0 2rem`

Current dashboard files:
@src/app/dashboard/page.tsx
@src/app/dashboard/components/kpi-section.tsx
@src/app/dashboard/components/bu-breakdown.tsx
@src/app/dashboard/components/chart-wrappers.tsx
@src/app/dashboard/components/recent-alerts-preview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page layout with gradient header</name>
  <files>
    src/app/dashboard/page.tsx
    src/app/dashboard/components/kpi-section.tsx
  </files>
  <action>
  1. **dashboard/page.tsx** - Replace inline styles with editorial layout:
     - Remove all `const xxxStyle: React.CSSProperties` inline style objects
     - Add a gradient header section at top: `bg-gradient-to-br from-secondary to-[#1a2332]` with white/paper text
       - Title: `font-display text-4xl font-light text-paper` ("Executive Dashboard")
       - Subtitle: `text-paper/80 text-lg mt-2` ("Real-time business intelligence...")
       - RefreshButton positioned top-right within header, styled for dark background
     - After header, content container: `max-w-[1400px] mx-auto px-8 py-8`
     - KPI section in header area: Move KPISection into the gradient header as stat cards (like reference `.header-stats`). Wrap in grid with `grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))` gap-8
     - Charts grid below header: `grid grid-cols-1 lg:grid-cols-2 gap-6`
     - BU Breakdown and Alerts in content area with editorial spacing (mb-8 between sections)
     - Skeleton fallbacks: Update from `bg-slate-200` to `bg-[var(--border)]` for editorial consistency

  2. **kpi-section.tsx** - Adapt for header placement:
     - Since KPIs may now live inside the gradient header, create two display modes or style the KPICard wrapper for dark backgrounds
     - Option A (simpler): Keep KPIs below header but style them with the editorial KPICard from Plan 01. Just update the grid gap and wrapper.
     - Option B (reference match): Move KPI summary stats into the header as light-on-dark stat cards like reference `.stat-card`
     - Choose Option A for simplicity (KPICard from Plan 01 already has editorial styling). Update wrapper to use `max-w-[1400px] mx-auto` container and `gap-6`
     - Update "Last updated" text: `text-sm text-muted mt-4`
  </action>
  <verify>
  - Run `npm run build` -- no errors
  - Dashboard shows gradient header (dark blue) with white title text in Cormorant Garamond
  - KPI cards below header have editorial styling (accent left border, serif values)
  - Page has max-width container with proper spacing
  </verify>
  <done>
  - Dashboard header has gradient from secondary (#2d4263) to dark (#1a2332) with paper-colored text
  - Title renders in Cormorant Garamond font-light at large size
  - Content area has 1400px max-width with editorial spacing
  - Skeletons use editorial border color instead of slate
  </done>
</task>

<task type="auto">
  <name>Task 2: BU breakdown, charts, and alerts with editorial styling</name>
  <files>
    src/app/dashboard/components/bu-breakdown.tsx
    src/app/dashboard/components/chart-wrappers.tsx
    src/app/dashboard/components/recent-alerts-preview.tsx
    src/app/dashboard/components/revenue-chart.tsx
    src/app/dashboard/components/margin-comparison.tsx
  </files>
  <action>
  1. **bu-breakdown.tsx** - Editorial BU cards:
     - Card wrapper: use `Card` component (already restyled in Plan 01)
     - Section title "Business Unit Breakdown": `font-display text-xl font-semibold text-secondary`
     - Each BU row: `border border-[var(--border)] rounded p-5 hover:border-accent transition-colors`
     - BU name: `font-display text-lg font-semibold text-secondary`
     - Revenue value: `text-2xl font-display font-bold text-ink`
     - Revenue mix bar: replace `bg-blue-500` with `bg-accent`, replace `bg-slate-400` with `bg-secondary`
     - Margin text: replace `text-green-600`/`text-red-600` with `text-[var(--success)]`/`text-[var(--critical)]`
     - Customer count badge: already uses Badge component (restyled in Plan 01)

  2. **chart-wrappers.tsx** - Editorial wrappers:
     - Wrap charts in Card component (already editorial from Plan 01)
     - Chart titles: `font-display text-lg font-semibold text-secondary`

  3. **revenue-chart.tsx** and **margin-comparison.tsx** - Update chart colors:
     - Primary chart color: `var(--accent)` (#c84b31) or `var(--secondary)` (#2d4263)
     - Use editorial palette for data series: secondary, accent, muted
     - Keep Recharts configuration intact, just update color values
     - Grid/axis text: `var(--muted)` (#8b8b8b)

  4. **recent-alerts-preview.tsx** - Editorial alert styling:
     - Replace `bg-red-50`/`bg-yellow-50` with `bg-[var(--critical)]/10`/`bg-[var(--warning)]/10`
     - Alert text: use `text-ink` for main text, `text-muted` for secondary
     - Border-left accent: `border-l-4 border-[var(--critical)]` for red, `border-l-4 border-[var(--warning)]` for yellow
     - "View all alerts" link: `text-accent hover:text-accent/80`
  </action>
  <verify>
  - Run `npm run build` -- no errors
  - BU breakdown cards show editorial styling with accent hover borders
  - Revenue mix bars use accent/secondary colors instead of blue/slate
  - Chart colors use editorial palette (secondary, accent)
  - Alert preview cards use editorial red/yellow from CSS variables
  </verify>
  <done>
  - All 5 dashboard sub-components use editorial theme tokens
  - BU cards: secondary headers, accent hover, editorial border
  - Charts: secondary/accent color palette, muted grid lines
  - Alerts: editorial red/yellow with left border accent
  - No functional changes to data fetching, calculations, or interactions
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Dashboard loads with gradient header, editorial KPIs, themed charts and BU cards
- No visual regressions in data display or interactivity
- All Suspense boundaries and error states still function
</verification>

<success_criteria>
1. Dashboard header: Gradient from secondary to dark, Cormorant Garamond title
2. KPI cards: Editorial styling with accent borders and serif values
3. BU breakdown: Accent hover states, secondary headers, editorial revenue bars
4. Charts: Secondary/accent color palette
5. Alerts preview: Editorial red/yellow with border-left accents
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-design-implementation/06-02-SUMMARY.md`
</output>
