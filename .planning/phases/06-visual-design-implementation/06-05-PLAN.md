---
phase: 06-visual-design-implementation
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/alerts/page.tsx
  - src/app/alerts/components/alert-summary.tsx
  - src/app/alerts/components/alert-list.tsx
  - src/app/scenario/page.tsx
  - src/app/scenario/components/scenario-form.tsx
  - src/app/scenario/components/impact-display.tsx
  - src/app/query/page.tsx
  - src/app/query/components/query-page-client.tsx
  - src/app/query/components/query-input.tsx
  - src/app/query/components/canned-queries.tsx
  - src/app/query/components/query-results.tsx
  - src/app/query/components/metrics-catalog.tsx
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Alerts page uses editorial red/yellow accent colors with border-left indicators"
    - "Scenario page has editorial form styling with accent submit buttons"
    - "Query page has editorial card-based layout with accent-colored canned query buttons"
    - "All secondary pages have consistent editorial header treatment"
    - "Human visually verifies the complete application matches reference design intent"
  artifacts:
    - path: "src/app/alerts/page.tsx"
      provides: "Alerts page with editorial header and accent-colored alert cards"
      contains: "font-display|accent|secondary"
    - path: "src/app/scenario/page.tsx"
      provides: "Scenario page with editorial header and form styling"
      contains: "font-display|accent"
    - path: "src/app/query/components/query-page-client.tsx"
      provides: "Query interface with editorial styling"
      contains: "font-display|accent|secondary"
  key_links:
    - from: "src/app/alerts/page.tsx"
      to: "src/app/globals.css"
      via: "Uses editorial CSS variables"
      pattern: "--accent|--critical|--warning"
---

<objective>
Restyle the three secondary pages (Alerts, Scenario Modeling, Natural Language Query) and the root page with the editorial theme, then perform human verification of the complete visual design across all pages.

Purpose: Complete visual consistency across the entire application. No page should feel like it belongs to a different design system.
Output: All remaining pages restyled + human verification that the full app matches reference design intent.
</objective>

<execution_context>
@/Users/RAZER/.claude/get-shit-done/workflows/execute-plan.md
@/Users/RAZER/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-visual-design-implementation/06-CONTEXT.md

Editorial design patterns established in Plan 01:
- Headers: gradient from secondary to dark, or solid secondary, with serif titles
- Cards: white bg, 1px border var(--border), shadow-sm, 4px radius
- Forms: border-[var(--border)], focus:border-accent focus:ring-accent
- Buttons primary: bg-accent text-white hover:bg-accent/90
- Section headings: font-display (Cormorant Garamond), text-secondary
- Body text: text-ink, secondary text: text-muted
- Alert/status: border-l-4 with --critical/--warning/--success

Current files:
@src/app/alerts/page.tsx
@src/app/alerts/components/alert-summary.tsx
@src/app/alerts/components/alert-list.tsx
@src/app/scenario/page.tsx
@src/app/scenario/components/scenario-form.tsx
@src/app/scenario/components/impact-display.tsx
@src/app/query/page.tsx
@src/app/query/components/query-page-client.tsx
@src/app/query/components/query-input.tsx
@src/app/query/components/canned-queries.tsx
@src/app/query/components/query-results.tsx
@src/app/query/components/metrics-catalog.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alerts, Scenario, and Query pages editorial restyling</name>
  <files>
    src/app/alerts/page.tsx
    src/app/alerts/components/alert-summary.tsx
    src/app/alerts/components/alert-list.tsx
    src/app/scenario/page.tsx
    src/app/scenario/components/scenario-form.tsx
    src/app/scenario/components/impact-display.tsx
    src/app/query/page.tsx
    src/app/query/components/query-page-client.tsx
    src/app/query/components/query-input.tsx
    src/app/query/components/canned-queries.tsx
    src/app/query/components/query-results.tsx
    src/app/query/components/metrics-catalog.tsx
    src/app/page.tsx
  </files>
  <action>
  Apply editorial theme consistently across all remaining pages. Each page gets a consistent header treatment and editorial component styling.

  **Alerts Page (3 files):**

  1. **alerts/page.tsx** - Editorial header:
     - Header section: `bg-gradient-to-br from-secondary to-[#1a2332] text-paper px-8 pt-12 pb-8`
     - Title: `font-display text-4xl font-light text-paper`
     - Subtitle: `text-paper/80 mt-2`
     - RefreshButton styled for dark background
     - Content: `max-w-[1400px] mx-auto px-8 py-8`
     - Skeleton: `bg-[var(--border)]` instead of slate

  2. **alert-summary.tsx** - Summary cards:
     - Replace slate styling with editorial tokens
     - Summary stat cards: `bg-white border border-[var(--border)] rounded p-5 shadow-sm`
     - Critical count: `text-3xl font-display font-bold text-[var(--critical)]`
     - Warning count: `text-3xl font-display font-bold text-[var(--warning)]`

  3. **alert-list.tsx** - Alert cards:
     - Each alert card: `bg-white border border-[var(--border)] rounded p-6 shadow-sm border-l-4`
     - Red alerts: `border-l-[var(--critical)]`, Yellow: `border-l-[var(--warning)]`
     - Alert title: `font-display text-lg font-semibold text-ink`
     - Description: `text-muted`
     - Impact value: `font-display font-bold`
     - Replace all `text-red-xxx` -> `text-[var(--critical)]`, `text-yellow-xxx` -> `text-[var(--warning)]`

  **Scenario Page (3 files):**

  4. **scenario/page.tsx** - Editorial header:
     - Same gradient header pattern as alerts
     - Title: "Scenario Modeling" in `font-display text-4xl font-light text-paper`
     - Content: `max-w-[1400px] mx-auto px-8 py-8`

  5. **scenario-form.tsx** - Editorial form styling:
     - Form inputs: `border-2 border-[var(--border)] rounded focus:border-accent focus:ring-1 focus:ring-accent`
     - Labels: `text-sm font-semibold text-ink`
     - Submit button: `bg-accent text-white px-6 py-3 rounded font-semibold hover:bg-accent/90 transition-colors`
     - Scenario type selector buttons: `border border-[var(--border)] hover:border-accent` active: `border-accent bg-accent/5 text-accent`
     - Form sections: editorial card wrappers

  6. **impact-display.tsx** - Editorial results:
     - Impact cards: `bg-white border border-[var(--border)] rounded p-6 shadow-sm`
     - Before/after metrics: `font-display text-2xl font-semibold` with color coding (success/critical)
     - AI analysis section: `bg-highlight border-l-4 border-accent p-6 rounded`

  **Query Page (6 files):**

  7. **query/page.tsx** - Minimal change (renders QueryPageClient)

  8. **query-page-client.tsx** - Editorial layout:
     - Header section: gradient header pattern
     - Title: "Business Intelligence Query" in `font-display text-4xl font-light text-paper`
     - Content layout: `max-w-[1400px] mx-auto px-8 py-8`

  9. **query-input.tsx** - Editorial query input:
     - Large input: `w-full px-6 py-4 text-lg border-2 border-[var(--border)] rounded-lg focus:border-accent focus:ring-1 focus:ring-accent`
     - Submit button: `bg-accent text-white`

  10. **canned-queries.tsx** - Editorial query cards:
      - Each canned query: `bg-white border border-[var(--border)] rounded p-4 cursor-pointer hover:border-accent hover:shadow-sm transition-all`
      - Category labels: `text-xs uppercase text-accent font-bold`
      - Query text: `font-display text-base font-semibold text-secondary`

  11. **query-results.tsx** - Editorial results:
      - Result card: `bg-white border border-[var(--border)] rounded p-6 shadow-sm`
      - Answer text: `font-display text-lg text-ink`
      - Confidence badge: use editorial success/warning colors
      - Source citations: `text-sm text-muted`

  12. **metrics-catalog.tsx** - Editorial catalog:
      - Collapsible header: `font-display text-lg font-semibold text-secondary`
      - Metric items: `bg-highlight/30 border border-[var(--border)] rounded p-3`
      - Metric names: `font-semibold text-ink`
      - Descriptions: `text-sm text-muted`

  **Root page (1 file):**

  13. **page.tsx** - Redirect or landing:
      - If it's a redirect to /dashboard, leave as-is
      - If it has content, apply editorial styling consistently
  </action>
  <verify>
  - Run `npm run build` -- no errors
  - Visit /alerts: gradient header, editorial alert cards with border-left accents
  - Visit /scenario: gradient header, editorial form with accent buttons
  - Visit /query: gradient header, editorial canned queries and results
  - All pages have consistent header treatment and typography
  </verify>
  <done>
  - All 13 files restyled with editorial theme
  - Consistent gradient header across Alerts, Scenario, and Query pages
  - Form elements use accent focus states
  - Alert cards use critical/warning border-left accents
  - Query interface has editorial card-based layout
  - No functional changes to any page logic
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete visual design implementation across all 6 pages (Dashboard, Accounts, Account Plans, Alerts, Scenario, Query) with editorial Paper/Ink theme, Cormorant Garamond + DM Sans typography, gradient headers, editorial cards, and reference-matching layouts.</what-built>
  <how-to-verify>
  1. Start the dev server: `npm run dev`
  2. Open http://localhost:3000/dashboard
     - Verify: Gradient header (dark blue), Cormorant Garamond title, editorial KPI cards
     - Verify: BU breakdown uses editorial colors (accent/secondary)
     - Verify: Charts have editorial color palette
  3. Navigate to http://localhost:3000/accounts
     - Verify: Gradient header with stat cards
     - Verify: Customer card grid with hover effects (lift + accent border)
     - Verify: Search bar centered, editorial styled
  4. Click a customer card (e.g., "British Telecommunications")
     - Verify: Hero header with gradient, stat cards, customer name in serif
     - Verify: Sticky tabs with accent active state
     - Verify: Click through all 7 tabs -- each has editorial styling
     - Verify: Action Items tab drag-and-drop still works
  5. Navigate to /alerts
     - Verify: Gradient header, alert cards with red/yellow border-left
  6. Navigate to /scenario
     - Verify: Gradient header, editorial form with accent buttons
  7. Navigate to /query
     - Verify: Gradient header, canned query cards with hover effects
  8. NavBar across all pages: dark ink background, rust-accent "SKYVERA" logo
  9. Overall: consistent Paper (#fafaf8) background, editorial typography, no residual blue/slate generic styling
  </how-to-verify>
  <resume-signal>Type "approved" if the design matches reference intent, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- All 6 pages render with editorial theme
- Human verifies visual quality across full application
</verification>

<success_criteria>
1. Alerts: Editorial gradient header, border-left accent alert cards
2. Scenario: Editorial form styling with accent buttons and focus states
3. Query: Editorial canned queries, results cards, metrics catalog
4. Consistency: All pages share gradient header, editorial typography, accent colors
5. Human approval: Visual quality verified by user across all pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-design-implementation/06-05-SUMMARY.md`
</output>
